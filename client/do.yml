name: frizz
region: fra
static_sites:
  - build_command: npm run build
    environment_slug: node-js
    github:
      branch: master
      deploy_on_push: true
    name: frizz
    output_dir: build
    routes:
      - path: /
        status: 200
        endpoint: index.html
    skip_build_if_exists: true
